This directory contains the `.sbatch` scripts for executing the benchmark evaluation jobs on a cluster.

**Important Note**: The `.sbatch` files need to be run from the `/scripts` directory to function properly.