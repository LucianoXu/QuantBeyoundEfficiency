# Benchmarking harness (pulls in transformers/torch/accelerate/numpy transitively,
# but we pin the ones we import directly below for reproducibility).
lm_eval[hf,math]==0.4.12
#--extra-index-url https://download.pytorch.org/whl/cu126 # uncomment for cuda inference
# Model loading and quantization
transformers==5.10.1
torch==2.12.0
#torch==2.12.0+cu126      # uncomment for cuda inference
accelerate==1.13.0        # required for device_map="auto"
bitsandbytes==0.49.2      # int8 / nf4 quantization (CUDA only)
optimum-quanto==0.2.7
llmcompressor==0.12.0     # AWQ (W4A16) calibration-based PTQ; CUDA/Linux only

# Config, env, and seeding
pyyaml==6.0.3
python-dotenv==1.2.1
numpy==2.2.6

#interpretability
#transformer-lens==3.4.0 # uncomment for optional judge results
#opena==2.44.0